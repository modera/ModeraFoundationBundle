framework:
    secret:          Hell yeah!
    csrf_protection: true
    session:         ~
    templating:
        engines: ['twig']

# Twig Configuration
twig:
    debug:            '%kernel.debug%'
    strict_variables: '%kernel.debug%'